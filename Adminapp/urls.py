from django.urls import path
from . import views

urlpatterns = [
    # path("connect/", views.connect, name='questions'),
    # path('question/', views.questions, name='questions'),
    # path('start/', views.start, name='questions'),
    # path('submit/<int:qn>/<str:ans>/', views.submit, name='questions')
]